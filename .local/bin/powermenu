#!/bin/sh

lock="󰌾 Lock Screen"
power="󰐥 Power Off"
reboot="󰑓 Reboot"
suspend="󰽥 Suspend"

option=$(printf "$lock\n$power\n$reboot\n$suspend" | rofi -dmenu -i -l 4 -p "Select an action" -theme themes/menu)

LOCK="hyprlock"

case $option in
"$power") systemctl poweroff ;;
"$reboot") systemctl reboot ;;
"$suspend") $LOCK & systemctl suspend ;;
"$lock") $LOCK ;;
*) ;;
esac
