#!/bin/sh

option=$(printf 'Lock Screen\nPower Off\nReboot\nSuspend' | bemenu --fixed-height -W 0.3 -c -i -l 4 -p "What do you want to do? ")
LOCK="i3slock"

case $option in
"Power Off") systemctl poweroff ;;
"Reboot") systemctl reboot ;;
"Suspend") $LOCK & systemctl suspend ;;
"Lock Screen") $LOCK ;;
*) ;;
esac
